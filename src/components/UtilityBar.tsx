// Intentionally empty. The utility bar was removed from the layout, but the
// module remains so any stale import resolves rather than breaking the build.
export default function UtilityBar() {
  return null;
}
